# config.js must be in the repo for GitHub Pages to serve it.
# It contains no secrets — Client ID is public by design, access is
# protected by Google OAuth + the ALLOWED_EMAILS whitelist.
# js/config.js

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
*.swp
*.swo
